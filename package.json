{
  "name": "use_sass",
  "version": "1.0.0",
  "description": "ESC Hacker Escape Rooms",
  "main": "index.js",
  "scripts": {
    "sass:translate": "sass src/style.scss main.css",
    "sass:watch": "sass --watch src/style.scss main.css",
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open"
    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.80.1"
  },
  "devDependencies": {
    "cypress": "^14.1.0"
  }
}
